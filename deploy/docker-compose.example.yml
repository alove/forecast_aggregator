services:
  election-forecast-collector:
    build: ..
    environment:
      EFC_CONTACT_EMAIL: "replace-me@example.com"
    volumes:
      - ./data:/data
    command: ["collect", "--output", "/data/election_forecasts_2026.csv", "--save-raw", "--raw-dir", "/data/raw_snapshots"]
